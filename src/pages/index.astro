---
/**
 * Root redirect page — detects browser language and redirects to /en/ or /zh-cn/.
 * This runs at build time to produce a static index.html with a client-side redirect.
 */
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Redirecting…</title>
    <script is:inline>
      (function () {
        var lang = (navigator.language || navigator.userLanguage || 'en').toLowerCase();
        var target = lang.startsWith('zh') ? '/zh-cn/' : '/en/';
        window.location.replace(target);
      })();
    </script>
    <meta http-equiv="refresh" content="0; url=/en/" />
  </head>
  <body>
    <p>Redirecting… <a href="/en/">Click here</a> if not redirected.</p>
  </body>
</html>
